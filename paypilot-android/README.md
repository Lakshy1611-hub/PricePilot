# PayPilot Android

Native Android payment-app foundation. Current build is sandbox/test mode only.

## Important
Live UPI bank transfers are not enabled. Production activation requires an authorized UPI/PSP/bank partner, required KYC/AML controls, security/compliance review, credentials, webhook verification and production onboarding.

The app must never display a real-payment success state unless the backend/provider has verified the transaction.
